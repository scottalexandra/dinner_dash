<h1>New order</h1>

<% if @items.empty? %>
  <p> Your cart is empty </p>
<% else %>
  <div class="items">
    <%= render partial: "items" %>
  </div>
  <div id="checkout">
      <%= link_to "Checkout", orders_path, :method => :post %>
  </div>
<% end %>
