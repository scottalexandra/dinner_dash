<% orders.each do |order| %>
<h3><%= order.status.capitalize %> orders</h3>
<div id=<%= order.id %>>
  <ul>
    <li>
      <%= link_to "Order #{order.format_order_number(order.id)}",
      order_path(order.id) %>
      Status: <%= order.status %>
    </li>
  </ul>
</div>
<% end %>
